<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.maternityapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignUpActivity" />
        <activity
            android:name=".PLActivity"
            android:label="@string/title_activity_pl"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".CalActivity" />
        <activity android:name=".HelpLineActivity" />
        <activity android:name=".DangerActivity" />
        <activity android:name=".MustKnowActivity" />
        <activity android:name=".RaActivity" />
        <activity android:name=".ToDoActivity" />
        <activity android:name=".firsttriActivity" />
        <activity android:name=".sectriActivity" />
        <activity android:name=".thirdtriActivity"></activity>
    </application>

</manifest>